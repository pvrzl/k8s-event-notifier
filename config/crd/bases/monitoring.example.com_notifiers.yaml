---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: notifiers.monitoring.example.com
spec:
  group: monitoring.example.com
  names:
    kind: Notifier
    listKind: NotifierList
    plural: notifiers
    singular: notifier
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Notifier is the Schema for the notifiers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotifierSpec defines the desired state of Notifier.
            properties:
              channel:
                description: Channel to use
                enum:
                - slack
                type: string
              defaultSettings:
                description: Default settings to apply if not provided
                properties:
                  enableVerbose:
                    description: Enable detailed logging of events in Slack messages
                    type: boolean
                  messagePrefix:
                    description: Prefix for messages (e.g., "[K8s Alert]")
                    type: string
                type: object
              eventReasons:
                description: Event reasons to filter notifications (e.g., ImagePullFailed,
                  CrashLoopBackOff)
                items:
                  type: string
                type: array
              eventTypes:
                description: Event types to notify on (e.g., Warning, Normal)
                items:
                  type: string
                minItems: 1
                type: array
              namespaces:
                description: Namespaces to monitor for events
                items:
                  type: string
                minItems: 1
                type: array
              webhook:
                description: Target webhook URL
                pattern: ^https?://.+
                type: string
            required:
            - channel
            - eventTypes
            - namespaces
            - webhook
            type: object
          status:
            description: NotifierStatus defines the observed state of Notifier.
            properties:
              lastEventTime:
                description: Last event processed timestamp
                format: date-time
                type: string
              observedGeneration:
                description: Current observed generation
                format: int64
                type: integer
              recentEvents:
                description: List of last processed events for debugging
                items:
                  type: string
                type: array
              statusMessage:
                description: Status message
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
